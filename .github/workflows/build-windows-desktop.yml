name: Build Linc Windows beta

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install desktop dependencies
        run: python -m pip install -r requirements-desktop.txt

      - name: Install bundled Chromium
        shell: pwsh
        run: |
          $env:PLAYWRIGHT_BROWSERS_PATH = "0"
          python -m playwright install chromium --only-shell

      - name: Build portable app
        shell: pwsh
        run: |
          $env:PLAYWRIGHT_BROWSERS_PATH = "0"
          python -m PyInstaller --noconfirm --clean packaging\linc.spec
          Copy-Item "templates\linc_tracker_template.xlsx" `
            "dist\Linc\Blank Linc Tracker.xlsx"
          Compress-Archive -Path "dist\Linc" `
            -DestinationPath "dist\Linc-v0.1.0-Windows.zip" `
            -CompressionLevel Optimal

      - name: Upload desktop beta
        uses: actions/upload-artifact@v7
        with:
          name: Linc-v0.1.0-Windows
          path: dist/Linc-v0.1.0-Windows.zip
          if-no-files-found: error
          retention-days: 14
